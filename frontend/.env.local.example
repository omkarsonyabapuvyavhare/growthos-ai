# GrowthOS AI frontend → FastAPI backend
#
# Local defaults used in this repo:
#   Backend often runs on http://localhost:8000  (uvicorn default)
#   Some setups use      http://localhost:8080
#
# Match this value to wherever you start the API, then restart `npm run dev`.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
